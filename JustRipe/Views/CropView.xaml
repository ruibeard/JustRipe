<UserControl x:Class="JustRipe.Views.CropView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:JustRipe.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Background="{Binding Color, FallbackValue=white}">
        <StackPanel>
            <Button Grid.Row="1" Content="Add Crop"
                    Command="{Binding AddCropCommand}" 
                    CommandParameter="hi there" 
                    Focusable="False"/>
            <TextBlock Text="{Binding Crop.Name}"/>
        </StackPanel>
    </Grid>
</UserControl>
